gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Exception : keystore DEBUG partagé (mot de passe standard "android", non
# sensible) — volontairement commité pour un SHA-1 debug identique sur toutes
# les machines. NE PAS committer un keystore de RELEASE ici.
!app/debug.keystore
